// import { find } from 'lodash'

const useGetMeta = (post, metaName) => {
  console.debug('post: ', post)
  console.debug('metaName: ', metaName)
  // return find(post.head.meta, meta => {
    // return meta.name === 'metaName'
    // return meta
  // }
}

export default { useGetMeta }